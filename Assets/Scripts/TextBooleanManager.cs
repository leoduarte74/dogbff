using System;
using UnityEngine;

public class TextBooleanManager : MonoBehaviour
{
    public static bool text_active;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        text_active = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
